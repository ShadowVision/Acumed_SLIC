// Generated by CoffeeScript 1.6.3
(function() {
  this.initEvents = function() {
    return listen("click", click);
  };

  this.click = function(e) {
    var d, id, success, _ref, _ref1;
    log(e.type + e.target);
    this.list = e.target.classList;
    id = e.target.id;
    d = new Date();
    window.lastClick = d.getTime();
    if (id === "menu-icon" || id === "font-size-icon") {
      switch (id) {
        case "menu-icon":
          toggle("menu");
          gotoTop();
          break;
        case "font-size-icon":
          toggle("settings");
      }
    } else {
      clearMenus();
      switch (id) {
        case "play-icon":
          togglePlaying();
          break;
        case "restart-icon":
          restartCurrent();
          break;
        case "autoplay-icon":
          toggleAutoPlay();
          break;
        case "set-zoom-small":
          setZoom("small");
          break;
        case "set-zoom-medium":
          setZoom("medium");
          break;
        case "set-zoom-large":
          setZoom("large");
          break;
        default:
          if (e.target.tagName === "IMG") {
            zoomImage(e.target);
          }
      }
    }
    if (((_ref = e.target.tagName) === 'A') && e.target.hash) {
      success = goto(e.target.hash);
      if (success) {
        e.stopPropagation();
        e.preventDefault();
        return false;
      } else {
        return true;
      }
    } else {
      if (((_ref1 = e.target.tagName) === 'BUTTON')) {
        e.stopPropagation();
        e.preventDefault();
        return false;
      }
    }
  };

}).call(this);
