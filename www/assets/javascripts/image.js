// Generated by CoffeeScript 1.6.3
(function() {
  this.zoomEnabled = true;

  this.zoomImage = function(target) {
    var image, path, url;
    if (this.zoomEnabled) {
      image = all("#image")[0];
      path = target.getAttribute("src");
      url = 'url(' + path + ')';
      image.style.backgroundImage = url;
      return add("image-zoom");
    }
  };

  this.unzoomImage = function() {
    return remove("image-zoom");
  };

}).call(this);
